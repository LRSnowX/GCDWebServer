name: Run Tests

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Xcode
        uses: actions/setup-xcode@v2
        with:
          xcode-version: '11.3'

      - name: Run tests
        run: |
          chmod +x Run-Tests.sh
          ./Run-Tests.sh
